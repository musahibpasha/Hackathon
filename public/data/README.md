Place the following CSV files in this folder so the app can load them at runtime:

- dataset.csv
- Symptom-severity.csv
- symptom_precaution.csv
- medicine_dataset.csv

During development copy the CSV files into `public/data/` (project root: ai-healthcare-system/public/data/) so they are available via `/data/<filename>`.
